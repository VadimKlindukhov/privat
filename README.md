1. вирішив імплементувати це через спринг евенти щоб не вигадувати додаткову логіку для цього
2. створюю запис у базі з датою, клієнтом та використаним лімітом, при запитах на створення нової заявки роблю лок, можна було б зробити відкат при помилках у провайдері але так як це не описано вирішив що його відсутність не буде помилкою
3. docker compose up --build

- Фронтенд http://localhost:5173
- Бекенд http://localhost:8080/api/v1/orders

фронтенд, тести та інфраструктуру делегував ШІ, не знаю на скільки часу розраховане завдання але для звичайного тестового завдання було би занадто робити все це своЇми руками
